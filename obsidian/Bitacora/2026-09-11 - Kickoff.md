# 2026-09-11 — Kickoff del proyecto

- Se recibió `NEGOCIOS_BATERIAS_ARAÑAS.xlsx` (270 negocios, hoja `ar_neg`) y `marcas_bat.xlsx` (catálogo de 119 marcas).
- Se definió el flujo de validación en 5 pasos (ver [[00 - Home]]).
- Se detectó que `NOMBRE NEGOCIO` (77/270 filas) y `DIRECCION` (270/270 filas) tienen encoding corrupto en el archivo original — ver [[Diccionario de Datos]]. El nombre se puede recuperar desde el `LINK`; la dirección solo vía reverse geocoding.
- Se creó estructura de repo, `.gitignore`, vault de Obsidian y skill de Claude Code para el flujo de validación.
- Pendiente: Ricardo comparte la API key de Google Maps → se agregan columnas nuevas al Excel (bloqueado momentáneamente porque el archivo estaba abierto en Excel) → se corre el pipeline sobre los primeros 10 CASE ID → se revisa en conjunto.
